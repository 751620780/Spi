	由于本程序是由VS2015开发的，故当程序安装在未安装vs2015的Windows上时，一般无法运行，
因为这个程序默认是动态链接编译的，但是很多依赖的dll只有安装了VS2015才会有(或者你手动添
加了这些dll)。因此你需要安装这些dll，也就是把这个目录的文件夹的内容放到安装目录(与
spi.dll放在一起)。或者你可以选择更改编译方式，将编译方式更改为静态链，接具体的办法如下：
1.“项目->右键属性->配置属性->常规->MFC的使用”选择成“在静态库中使用 MFC”
2.“项目->右键属性->配置属性->C/C++->代码生成->运行库”选择“多线程 (/MT)”